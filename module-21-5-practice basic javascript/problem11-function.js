// 11. write a function that takes three number and returns multiplication of the three number.

function getMultiplication(num1, num2, num3) {
     let multipliation = num1 * num2 * num3;
     return multipliation;
}
let total = getMultiplication(2, 2, 2,);
console.log(total);

let ammuNeed = getMultiplication(5, 5, 5);
console.log(ammuNeed);